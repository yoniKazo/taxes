# IMPLEMENTATION.md

טבלה חיה — מתעדכנת תוך כדי ביצוע כל artifact, לא בלילה לפני הגשה (M13).

| ID | Concept | Tier | Where | Why |
|----|---------|------|-------|-----|
| M1 | CLAUDE.md — constitution | Must | `CLAUDE.md` | הפרויקט כבר צבר החלטות שקל לשחזר לא נכון (למשל: alias לא מתוארך למודל Gemini אחרי ש-`gemini-2.0-flash` קרס ב-404; הרצה בודדת בלבד מול המודל המקומי) — CLAUDE.md מונע חזרה על אותן טעויות בסשן הבא. |
| M2 | שני rules, שני scopes | Must | `.claude/rules/hosted-llm-quota.md` (always-on), `.claude/rules/tax-data-sourcing.md` (path-scoped ל-`data/**/*.md`) | ה-throttling ל-Gemini הוא constraint גורף לכל סקריפט (always-on); דרישת sourcing למספרי מס רלוונטית רק לקבצי `data/*.md` — לכן שני scopes נפרדים, לא כלל כללי מדי אחד. **עדכון ממטלה 3:** ה-scope הצר הזה הפך למגבלה נראית — קורפוס ה-RAG (`TaxData/`) יושב מחוץ לתיקיית הפרויקט, ולכן path-scoped rule לא יכול להגיע אליו בכלל. זו לא טעות בכלל אלא גבול של המנגנון, והוא מתועד כעת בשני הקצוות (ב-rule עצמו וב-Open questions של `CLAUDE.md`) במקום להישאר ידע שבעל-פה. |
| M3 | Skill — add-llm-script | Must | `.claude/skills/add-llm-script/SKILL.md` | הדפוס (client setup, utf-8 stdout, קילוף JSON fence) כבר חזר 3 פעמים בקוד (`hello_llm.py`, `file_qa.py`, `qa_experiment.py`) בלי שהיה מתועד במקום אחד — סיכון ממשי שסקריפט רביעי ישכפל טעות (למשל ישכח את ה-encoding fix) במקום לעקוב אחרי הדפוס שכבר עבד. |
| M4 | Subagent — groundedness-reviewer | Must | `.claude/agents/groundedness-reviewer.md` | tools מוגבל בכוונה ל-`Read, Grep, Glob` (בלי Edit/Write/Bash) כדי שסקירת hallucination תישאר read-only — לא רוצים שסוכן שתפקידו לבקר יוכל בטעות "לתקן" את `data/tax_notes.md` תוך כדי ביקורת. |
| M5 | Spec-first feature — מחשבון מס והחזר לשכיר | Must | `specs/tax-refund-calculator.md` (commit `e32fee4`) → `src/tax_refund_calculator.py` (commit `f548f4c`) | ה-spec נכתב ונכנס ל-git **לפני** שורת קוד אחת של המימוש (שני commits נפרדים, לפי הסדר) — כדי לתפוס אי-התאמות (למשל תבנית EARS "Optional feature" מול "Complex") לפני שהן מתקבעות בקוד. 5 קריטריוני EARS + out-of-scope מפורש + test vectors שנשלפו מדוגמאות מחושבות שכבר קיימות ומצוטטות ב-`data/tax_notes.md` (לא sourcing חדש), ואומתו בפועל מול המימוש — 4/4 עברו. |
| M6 | CODIFY log — 11 כשלים אמיתיים מתועדים | Must | סעיף `## CODIFY log` ב-`CLAUDE.md` (10 entries) + `.claude/rules/hosted-llm-quota.md` (1 entry) | כל entry מתועד כי שינה התנהגות בפועל: `gemini-2.0-flash` קרס ב-404 (מודל מתוארך), cp1255 קרס על עברית ב-Windows, Gemini עטף JSON ב-fence, ומודלים non-lite נתקלים בתקרת 20 req/יום (לא 15/min כמו lite). פוצל בין CLAUDE.md ל-rules כי ה-checklist מפרש "CLAUDE.md **+** rules" (רבים) — לא קובץ יחיד. **עודכן במטלה 3 (2026-08-18→20):** ה-log גדל מ-4 ל-11 entries, וזו הנקודה של M6 — הוא נכתב תוך כדי עבודה ולא באיסוף רטרואקטיבי. השישה שנוספו הם כשלים שכולם **החזירו תשובה שגויה בשקט** במקום לזרוק שגיאה: גרשיים עבריים ששוברים JSON, תווית ground-truth שנקבעה מ-grep חלקי, 4 קריאות שופט מול throttle יחיד, שופט שנבנה בלי לראות את השאלה, `L2` שהוצג כדמיון, וציטוט מקובץ (`[1, 2, 8]`) שחמק ממשמר הציטוטים. כל אחד מהם שינה קוד. |
| M7 | Slash command — /mark-checklist-item | Must | `.claude/commands/mark-checklist-item.md` | הטריגר האמיתי: לאורך Session 1–3 עדכנתי checkbox ב-`HomeWork/agentic-infra-tasks.md` ושורה ב-`IMPLEMENTATION.md` ידנית בסוף כל פריט (M1–M4, M5, M6 — כולל כ-commit נפרד `1c1c66a`) — task חוזר בפועל, לא היפותטי. הפקודה מקבלת `$ARGUMENTS` (מזהה פריט), קוראת git log/diff כדי להסיק Concept/Tier/Where/Why במקום הקלדה ידנית חוזרת, ומבצעת את שתי העריכות. במכוון לא עושה git commit — זו החלטה נפרדת, בהתאם לכך ש-`contract.md` (M12, Session 4) עדיין לא מגדיר commit כ-CAN-without-approval. **הערה:** ניסיתי להריץ אותה בפועל דרך ה-Skill tool כבדיקה חיה (`Skill(skill="mark-checklist-item", args="M8")`) — נכשלה עם "Unknown skill", כי `.claude/commands/` לא נסרק דינמית לאותה רשימת skills בסשן הנוכחי (בניגוד ל-`.claude/skills/`, שכן נסרק). שורות M7/M8 הללו בכל זאת נערכו ידנית, לפי אותו תהליך שהפקודה מקודדת — בדיקה חיה אמיתית (דרך `/mark-checklist-item` שהמשתמש מקליד) עדיין פתוחה לפעם הבאה. |
| M8 | MCP decision — server הוגדר בסוף (X1) | Must | `.mcp.json` + `mcp_servers/tax_corpus.py` (commit `ac650e8`) | דילוג נשקל ונדחה **פעמיים** לפני שהתקבל: תחילה (Session 1) לא היה בכלל מה לעטוף מלבד שני קבצי md קטנים; אחרי מטלה 3 (קורפוס 6 מסמכים) עדיין נדחה כי ה-RAG כבר נחשף כ-HTTP API נבדק. ההחלטה התהפכה רק כש-X1 זיהה שימוש **שונה במהות**: לא "עטוף את ה-API הקיים" (שכבה כפולה, אנטי-פאטרן #6 בchecklist) אלא "תן ל-Claude Code עצמו לתשאל את הקורפוס תוך כדי עבודה" — צורך שה-HTTP API לא פותר כי הוא מיועד לדפדפן, לא לסוכן. שלושה tools צרים (לא tool גנרי אחד), 0 secrets ב-`.mcp.json` (retrieval מקומי לגמרי). |
| M9 | PreToolUse hook — חוסם דליפת `.env` | Must | `.claude/hooks/block_env_leak.py` + חיווט `PreToolUse` ב-`.claude/settings.json` (commit `bf65622`) | Forbidden #1 ב-`CLAUDE.md` היה עד כה **הוראה בלבד** — כלומר הוא תלוי בכך שהמודל יקרא אותה, יבין אותה ויציית לה בכל פעם מחדש. ההוק הופך אותו למנגנון: קורא את קריאת הכלי מ-stdin ומחזיר `exit 2` על קריאת `.env` ב-Bash (`cat`/`type`/`more`/`less`/`Get-Content`/`gc`), על `git add`/`git commit` שלו, ועל `Edit`/`Write` ישיר אליו; `.env.example` מוחרג ב-regex (`\.env\b(?!\.example)`) כדי שהכלל לא יחסום את הקובץ שדווקא **צריך** להיכנס לגיט. **נבדק בפועל בשתי דרכים** (מתועד בגוף ה-commit): הזנת JSON ישירות ל-stdin (exit 2 על מסוכן, exit 0 על בטוח), ובנוסף ריצת `claude -p` headless מול הפרויקט — שם המודל **סירב מעצמו** על סמך CLAUDE.md לפני שהגיע בכלל לקריאת הכלי. זו התוצאה המעניינת: שכבת ההוראה עבדה, וההוק הוא מה שהיה תופס אילו לא עבדה. |
| M10 | PostToolUse audit hook — JSONL לכל כתיבה | Must | `.claude/hooks/audit_log.py` + חיווט `PostToolUse` (matcher `Write\|Edit\|Bash`) ב-`.claude/settings.json` (commit `67c06f4`); יומן ב-`.claude/hooks/audit.jsonl` | **שני תיקונים אמיתיים, לא הרחבה קוסמטית.** (1) ה-matcher היה `Write\|Edit` בלבד — קבצים שנערכים דרך `Bash(python ...)`, דפוס נפוץ כאן, לא תועדו כלל. הורחב ל-`Write\|Edit\|Bash` עם heuristic של write-signal (`python`/`mv`/`cp`/`rm`/`tee`/redirect) כדי לא להציף ב-`git status`/`ls`. (2) ה-entry היחיד שהיה קיים ביומן (מ-18.8) הצביע על נתיב scratchpad זמני מחוץ לריפו — כלומר **הוק שנבדק פעם אחת ידנית, לא יומן שנצבר בעבודה אמיתית**, בדיוק המקרה שה-checklist פוסל במפורש. נמחק. **אומת חי בפועל, לא רק ב-pipe-testing**: הרצת `claude -p "/mark-checklist-item M12" --permission-mode acceptEdits` מ-`tax-copilot/` (ראו הערת M7 למטה) הצליחה בסופו של דבר לייצר **entries אמיתיים**: `{"tool":"Bash",...}` על פקודת `find` (הקו הנטוי ב-`2>/dev/null` נתפס כ-write-signal, כצפוי) ואז `{"tool":"Edit","path":"...HomeWork\\agentic-infra-tasks.md"}` על עריכה אמיתית שהתבצעה. זו הפעם הראשונה שהיומן מכיל שורות שנוצרו על ידי session חי ולא ידנית — סוגר את הפער שנפתח קודם. |
| M11 | Permissions policy — `defaultMode` + allow/ask/deny | Must | `.claude/settings.json` (commit `bf65622`, deny הורחב ב-`f796210`) | שלוש הרמות ממופות לשלוש רמות סיכון אמיתיות בפרויקט, לא לתבנית גנרית. **allow**: קריאה/חיפוש (`Read`/`Grep`/`Glob`), `Bash(python *)`, `pytest`, וקריאות git קריאה-בלבד — כל מה שהתבקש אישור עליו עשרות פעמים ביום בלי שאי-פעם הייתה סיבה לסרב. **ask**: `Edit`/`Write`, `git commit`/`git push`, `pip install` — פעולות שמשנות מצב מתמשך (עץ עבודה, היסטוריה, סביבה). **deny**: כל וריאנטי קריאת `.env` ו-`git add .env`, בחפיפה מכוונת ל-M9, `rm -rf*`, ו-**`Write`/`Edit` על `assignment3/index/**`** (נוסף בזמן כתיבת M12 — האינדקס הקנוני היה מתועד כ"read-only" ב-CLAUDE.md בלי אכיפה מכנית בפועל). **החפיפה עם ה-hook אינה כפילות אלא שתי שכבות שנכשלות אחרת**: ה-deny הוא רשימת דפוסים שנעקפת על ידי כל ניסוח שלא חשבתי עליו, וההוק הוא regex על תוכן הפקודה בפועל שרץ אחרי שההרשאה כבר ניתנה. `defaultMode: "default"` נשמר במכוון — לא `acceptEdits` — כדי ש-`ask` על `Edit`/`Write` באמת ייאכף. |
| M12 | Agent Responsibility Contract | Must | `contract.md` (commit `f796210`, תוקן ב-`f88fcba`) | נכתב **מלמטה למעלה** מ-M9/M11 בפועל: כל שורת FORBIDDEN חייבת twin מכני (deny entry או בדיקת hook), אחרת אינה נכנסת לרשימה — כלל שהעיף בפועל את "אל תחליף מכשיר מדידה באמצע ניסוי" (CLAUDE.md Forbidden #5) החוצה, כי אין דרך מכנית לזהות שעריכה משנה התנהגות שופט; היא נשארת convention מפוקחת בסקירה, לא כלל contract. **תוקן פעם אחת אחרי כתיבה** — הפניה שגויה ל-"Forbidden #1" (במקום #5) עבור כלל החלפת המכשיר, שנתפסה על ידי X7 (graphify) כ-AMBIGUOUS edge בין שני קבצים שמצטטים אותו מספר לשני כללים שונים. Escalation rule: עצירה אחרי 3 כשלי tool ברצף, ואחרי `RateLimitError` שחוזר פעמיים (המכסה היומית כבר נשרפה פעם, `phaseb_run.log`). |
| M13 | IMPLEMENTATION.md | Must | `IMPLEMENTATION.md` (קובץ זה) | הטבלה עצמה החזיקה 11 שורות בלבד (M1–M11) עד לסבב הזה — 16 חסרות (M12, M13, S1–S7, X1–X7), כולל שורות עבור פריטים שדולגו במכוון שהיו קיימים רק בסעיף "Skipped" ולא בטבלה, למרות שה-Implementation Guide דורש שורה לכל אחד מ-27 הפריטים בטבלה עצמה. עודכן בסוף כל שלב עבודה (12 commits בין ה-`git log` של הענף), לא במכה אחת. |
| S1 | Plan Mode בשינוי הגדול ביותר | Should | `plans/2026-08-18-assignment3-rag.md`, `plans/2026-08-20-rag-lab-ui.md` (commit `cca01f2`) | השינוי הגדול ביותר אומת מול `git log --stat`: commit `39692df` (RAG + RAG Lab UI) — 122 קבצים, 15,398 שורות, מול 5,534 של הקליינט ו-135 של תיקוני ה-review. שני ה-plans נשמרו **לפני** אותו commit (18.8 ו-20.8, ה-commit מ-22.8). ה-Evidence אינה קיום הקובץ אלא שתי דחיות מתועדות בגוף ה-plan: כתיבה ל-`assignment3/index/` הקנוני מה-UI נדחתה מראש (הועברה ל-`index_custom/`), והרצת מערך ההערכה המלא מה-UI נדחתה בגלל מכסת 500 קריאות שכבר נשרפה. |
| S2 | Nested CLAUDE.md | Should | `api/CLAUDE.md` (commit `12ec910`) | שני מועמדים קודמים (`data/`, `web/`) נפסלו כי כבר מכוסים במלואם ע"י `tax-data-sourcing.md` ו-`web/README.md` בהתאמה — כפילות הייתה יוצרת שני מקורות אמת. `api/` עמד במבחן שהפיל את שניהם: יש בו context local אמיתי (פיצול `api/rag/` לפי עלות LLM, שלושה תיקוני CODIFY על ציוני דמיון/hybrid retriever, lazy client) שלא היה מתועד באף מקום אחר, והוסר מ-`CLAUDE.md` השורש כדי שלא ייטען בכל session ללא צורך. |
| S3 | `/code-review` לפני PR | Should | `reviews/code-review-2026-08-16.md` (commit `ed26e84`) | 8 הממצאים (commit `825c2c0`) תוקנו כולם, אבל התיעוד חי רק בגוף ה-commit — אין שורה ב-IMPLEMENTATION.md ואין קובץ ממצאים נפרד, למרות שה-Evidence דורש גם מה-נדחה. נכתב עכשיו במפורש: כל 8 תוקנו, אף אחד לא נדחה, ושניים מהם (הסרת error handling מת, קיצור docstrings) הם הסרה שאוכפת את `CLAUDE.md` עצמו. |
| S4 | `/security-review` על branch diff | Should | `reviews/security-review-2026-08-23.md` (commit `ab438d6`) | ממצא אחד אמיתי: הרחבת ה-audit hook לקריאות Bash (M10, `67c06f4`) פתחה נתיב דליפת secrets — פקודה עם `KEY=value` מוטבע (הדפוס שהפרויקט עצמו כבר השתמש בו ב-`settings.local.json`) הייתה נכתבת מילולית ל-`audit.jsonl` הנכנס לגיט. תוקן **באותה סקירה**: `_redact()` מחליף `NAME=value` ב-`NAME=***` לפני כתיבה, אומת בפועל מול פקודה עם מפתח מזויף. תוקן גם בעותק ב-`tax-copilot-plugin/`. אפס ממצאים נדחו. |
| S5 | Headless run | Should | `scripts/tax_constants_drift.py` (commit `9deed11`) | פותר Open Question קיים ב-CLAUDE.md: `tax_refund_calculator.py` מקשיח מדרגות מס כקבועים, ו-`tax-data-sourcing.md` לא חל על `.py` — אף מנגנון לא תופס דריפט אם `tax_notes.md` יתעדכן. הסקריפט מריץ `claude -p ... --output-format json`, מפרסר את התשובה, `exit 1` על דריפט. **אומת בשני הכיוונים בפועל**: ריצה נקייה (`exit 0`, עלות $0.34, 4 turns), ואז קבוע מזויף בכוונה (`242.0`→`999.0`) שנתפס נכון (`exit 1`) והוחזר במדויק דרך `git checkout`. |
| S6 | Claude Code ב-CI | Should | `.github/workflows/claude.yml` (commit `e4643e3`, תוקן ב-`1e7f4e5`) | נכתב תחילה בלי להיות מופעל בפועל; הושלם ב-2026-08-23 אחרי שהתווסף `ANTHROPIC_API_KEY` כ-repo secret וחשפה שני מחסומים נוספים שלא נראו מקריאת ה-YAML בלבד. (1) **`secrets` context לא זמין בתוך `jobs.<id>.if`** — GitHub Actions דוחה אותו עם "Unrecognized named-value: 'secrets'"; הפתרון: job נפרד `check-key` שקורא את ה-secret כ-`env` בתוך step (שם `secrets` כן מותר) וכותב `has-key` ל-`GITHUB_OUTPUT`, וה-job `claude` נשער מול `needs.check-key.outputs.has-key` במקום מול `secrets` ישירות. (2) גם עם secret תקין ו-syntax תקין, ה-action נכשל ב-`401 Unauthorized — Claude Code is not installed on this repository`: `claude-code-action` דורש גם התקנת אפליקציית **Claude Code GitHub App** על הריפו (ל-OIDC token exchange), לא רק את ה-secret. **אומת חי מקצה לקצה**: PR #3, run `32648364891` — שלושת ה-jobs (`tests`, `check-key`, `claude`) ירוקים. `[x]` ב-checklist עודכן בהתאם. |
| S7 | Cost discipline | Should | סעיף זה למטה | ראו "Cost discipline" בתחתית הקובץ. |
| X1 | Custom MCP server | Stretch | `mcp_servers/tax_corpus.py`, `.mcp.json` (commit `ac650e8`) | שלושה tools צרים (לא tool גנרי אחד): `search_tax_corpus` (סמנטי, 0 קריאות LLM — משתמש ב-`api/rag/retrieval.get_vectorstore` הקיים), `list_corpus_documents`, `get_chunk`. במכוון בלי tool ליצירת תשובה — זה כבר חשוף כ-HTTP API נבדק, וסוכן לא-מתוזמן שמייצר תשובות היה שורף מכסה בלי בקרה. **אומת בפועל** עם `GEMINI_API_KEY` מוסר מהסביבה: 6 מסמכים, retrieval רלוונטי ל"נקודת זיכוי", אפס קריאות Gemini. הפך את החלטת M8 מדילוג לביצוע. |
| X2 | Spec Kit run | Stretch | `.specify/` (commit `38abb55`) → `specs/001-judge-version-stamping/{spec,plan,tasks}.md` (commit `9dd55a7`) → מימוש ב-`src/judges.py`+`src/assignment3_evaluate.py`+`tests/test_judge_version.py` (commit `411bfe7`) | `specify init --integration claude` (uvx, GitHub's spec-kit) הותקן אמיתי — 10 skills + תשתית `.specify/`. הפיצ'ר שנבחר אינו היפותטי: `judge_version` מופיע גם ב-"What I'd do differently" הקודם וגם כ-Open Question פתוחה (דלתא לא-להשוואה בין `exp1_top_k_8_judged.json` לשופט הישן). `_compute_judge_version()` מחשב hash מ-4 פרומפטי השופט — עריכת פרומפט משנה את הגרסה אוטומטית, בלי bump ידני לשכוח. Constitution Check ב-`plan.md` נבדק מול `CLAUDE.md` האמיתי (לא template ריק של spec-kit), וקורא במפורש ל-Forbidden #5 (לא להחליף מכשיר מדידה) כדי להבהיר ש-34 השורות הקיימות **לא** נשפטות מחדש. **סדר spec-first נשמר** (spec+plan+tasks לפני קוד, commits נפרדים). 5/5 טסטים עברו (3 טהורים + 2 שדרשו torch, שהיה חסום זמנית תחת עומס זיכרון בסביבה — לא קשור לקוד, אומת אחרי שהזיכרון התפנה). |
| X3 | Dynamic Workflow | Stretch | `.claude/workflows/corpus-sourcing-audit.js` (commit `28cb7f3`) | טריגר אמיתי: `tax-data-sourcing.md` הוא path-scoped ל-`data/**/*.md` ולא חוצה את גבול תיקיית הפרויקט, ולכן `TaxData/` (4 תחומי מס נוספים) חשוף בלי sourcing אוטומטי — מגבלה שכבר מתועדת ב-Open Questions. `pipeline()` לקריאת המניפסט → `parallel()` (לא ברירת המחדל, כי שישה מסמכים באמת בלתי-תלויים) של `groundedness-reviewer` הקיים לכל מסמך → `pipeline()` איחוד. `agent()`/`pipeline()` אומתו מול התיעוד הרשמי (code.claude.com/docs/en/workflows); `parallel()`/`agentType` מגובים ב-3 מדריכים חיצוניים בלבד, לא בעמוד רשמי — מתועד ככה בקובץ. |
| X4 | Agent Team run | Stretch | — | **לא בוצע.** התכונה (teammates חולקים working tree, file-ownership clause) לא נגישה דרך משטח הכלים הזמין בסשן הזה — אין הבחנה בין Agent tool רגיל (שמפעיל subagent בתוך אותו session, לא teammate נפרד עם coordination) לבין Agent Team האמיתי. דולג בכנות במקום להעמיד פנים שהורץ. |
| X5 | Plugin | Stretch | `tax-copilot-plugin/` (commit `8979ffe`) | אורז מחדש M3/M4/M7/M9/M10 תחת `.claude-plugin/plugin.json` (`version: 0.1.0`). מבנה התיקיות ומיקום `hooks/hooks.json` (ב-root של ה-plugin, לא בתוך `.claude-plugin/`) אומתו מול code.claude.com/docs/en/plugins-reference — לא מהזיכרון. **החלטה מוצהרת ב-README של ה-plugin עצמו**: זו אריזה מקבילה, לא מקור האמת — `.claude/` החי בשורש הפרויקט נשאר מה שבאמת רץ, וה-tradeoff (סנכרון ידני, שכבר כמעט נשכח פעם אחת ב-S4 כשתיקון האבטחה תוקן במקור אך כמעט לא הועתק לעותק) מוצג במפורש במקום מוסתר. |
| X6 | Background run | Stretch | — | **`/goal`, לא `/loop`** — ותועד למה: יש כאן finish line בינארי ובדיק ("`pytest` עובר בלי `GEMINI_API_KEY` בסביבה"), ו-`/loop` מיועד למוניטורינג שלעולם לא נגמר, לא למשימה עם תנאי סיום ברור. ה-finish line **אינו היפותטי** — הוא כבר נשבר פעם: `llm.py` בנה לקוח OpenAI בזמן import, והפיל את uvicorn בלי מפתח (CODIFY 2026-08-20). **אומת ישירות**: 77/77 טסטים עברו עם `GEMINI_API_KEY` מוסר מהסביבה. `/goal` עצמו לא ניתן להפעלה ממשטח הכלים הזמין בסשן הזה (feature ייחודי ל-CLI האינטראקטיבי) — התיעוד וה-finish line מלאים, ההרצה בפועל ממתינה לסשן אינטראקטיבי. |
| X7 | Codebase knowledge graph | Stretch | `graphify-out/` (commit `1bb5cc7`) | נבנה על 187 קבצים (133 קוד + 53 doc + 1 image): AST 854 nodes/2109 edges + semantic (3 subagents במקביל, ללא `GEMINI_API_KEY`) 334 nodes/330 edges → 1167 nodes, 2053 edges, 132 communities. **הquery שגרep היה מפספס — ולא היפותטי**: תוך כדי labeling, ה-extraction הסמנטי דגל edge כ-AMBIGUOUS בין שני קבצים שמצטטים "Forbidden #1 ב-CLAUDE.md" לשני כללים שונים. grep על המחרוזת היה מראה שתי ההתאמות כעקביות; רק מעקב אחר מה כל ציטוט **מצביע עליו בפועל** תפס את הפער — זה תוקן ב-`f88fcba`. אומת גם ש-`answer_with_rag_instrumented` (rag_pipeline.py) מחובר דרך `api/rag/generation.py` ל-`web/src/api/client.js`'s `request()`, חוצה בדיוק את גבול ה-HTTP/JSON ש-S2 מתעד. **מגבלה גלויה**: health check דיווח 365 dangling-endpoint edges מתוך 2439 (לא-קטלני, צפוי מ-extraction מקבילה ב-3 chunks) — לא משפיע על שני הממצאים למעלה, ששניהם עוברים דרך AST edges. תיוג community נעשה ידנית ל-21 הגדולים בלבד; 111 הקטנים נשארו עם label ברירת מחדל. |

## Cost discipline (S7)

- **Model routing**: `gemini-flash-lite-latest` לגנרציה, `gemini-3.1-flash-lite` לשיפוט — שניהם lite במכוון, כי non-lite חסום ב-20 בקשות/**יום** (לא 15/דקה כמו lite), מלכודת שנשרפה פעמיים (CODIFY 2026-08-13). `bloomz-560m` המקומי רץ פעם אחת בלבד — כל קריאה לוקחת דקות על CPU.
- **מספר אמיתי #1**: מכסת 500 קריאות/יום שנשרפה באמצע Task 6 שלב ב' — `exp2` נעצר אחרי גנרציה, `exp3` לא רץ כלל ([phaseb_run.log](assignment3/data/phaseb_run.log)).
- **מספר אמיתי #2**: ריצת S5 הנקייה עלתה **$0.34** (4 turns); הריצה עם הדריפט המכוון עלתה $0.25 (4 turns) — שתיהן נמדדו בפועל, לא מוערכות.
- **`clear` מול `compact`**: `compact` בתוך משימה מתמשכת ששומרת החלטות (למשל כל סבב ה-27 הפריטים הזה); `clear` במעבר בין מטלות נפרדות (assignment1→2→3).

## Skipped, and why

- **X4 — Agent Team run**: לא בוצע. הפיצ'ר (teammates חולקים working tree, file-ownership clause) לא נגיש דרך משטח הכלים הזמין בסשן הזה — יש הבחנה בין subagent רגיל (Agent tool, רץ בתוך אותו session) לבין Agent Team אמיתי (teammates נפרדים עם coordination על עץ עבודה משותף), וכלי ה-Agent הזמין הוא הראשון. דולג בכנות במקום לתייג subagent רגיל כאילו הוא Agent Team.
- **X6 — הרצת `/goal` בפועל**: התיעוד (הבחירה, הנימוק מול `/loop`, ה-finish line) מלא, וה-finish line עצמו אומת ישירות (`pytest` ירוק בלי `GEMINI_API_KEY`) — אבל `/goal` הוא feature ייחודי ל-CLI האינטראקטיבי, לא נגיש ממשטח הכלים של סשן זה. הריצה בפועל ממתינה לסשן אינטראקטיבי.
- **`/mark-checklist-item` (M7) — הופעלה חי, הצליחה חלקית, והתוצאה מעניינת יותר מהצלחה מלאה.** `claude -p "/mark-checklist-item M12" --permission-mode acceptEdits` מ-`tax-copilot/`: העריכה ל-`HomeWork/agentic-infra-tasks.md` (מחוץ לתיקיית הפרויקט) **עברה בפועל** ותועדה ביומן ה-audit האמיתי (ראו M10). העריכה ל-`IMPLEMENTATION.md` (בתוך תיקיית הפרויקט, מוגנת ב-`ask` לפי M11) **נדחתה** — כי workspace לא-מאומן ב-headless mode לא יכול להציג prompt אינטראקטיבי, אז `ask` נופל ל-deny בטוח (fail-closed), לא ל-allow שקט. זו לא תקלה — זו בדיוק ההתנהגות הנכונה של M11 תחת session לא-מאומן, ותוצאה טובה יותר מ"עבד בשקט על שני הקבצים בלי לבדוק את ה-permission boundary".

## What I'd do differently next time

- **הייתי בונה את ה-harness (M9–M11) בסשן הראשון, לא ברביעי.** מטלה 3 היא זו שהוכיחה את זה: ההגנות שנכתבו כהוראות ב-CLAUDE.md החזיקו כל עוד זכרתי אותן, ונשברו בשקט כשלא. הדוגמה החדה ביותר מתועדת ב-`assignment3_writeup.md` — הכלל "לא להחליף מכשיר מדידה באמצע ניסוי" הופר בתוך פחות משעתיים מרגע שניסחתי אותו, כי `judge_rag_row` מייבא את הקוד העדכני בכל ריצה ואף שכבה מכנית לא עצרה את זה. **ההבדל בין M9 (מנגנון) ל-Forbidden ב-CLAUDE.md (הוראה) הוא בדיוק ההבדל שם.**
- **הייתי מקבע גרסת מכשיר לצד כל תוצאה שנשמרת — ובוצע, סוף סוף, ב-X2.** כל קובץ תוצאה במטלה 3 שמר פלט בלי גרסת ה-prompt של השופט, והשוואת שתי ריצות דרשה ארכיאולוגיה של חותמות זמן. `judge_version` (hash מ-4 פרומפטי השופט, לא bump ידני) נכנס בסוף — אבל רק כי X2 (Spec Kit) נתן לזה מקום; זו הייתה יכולה להיכתב במטלה 3 עצמה בעלות של שורה אחת.
- **ה-audit של M10 תיעד כלים, לא שינויים — ותוקן, אבל אימות ה-live-fire קרה כמעט במקרה, קרוב לסוף.** ה-matcher הורחב מ-`Write|Edit` ל-`Write|Edit|Bash` מוקדם בסבב הזה, אבל היומן נשאר ריק (0 entries) לאורך כל שאר העבודה, כי כל commit נעשה מ-session שה-cwd שלו הוא שורש הריפו, לא `tax-copilot/` — בדיוק המגבלה שהזוהתה בתחילת התכנון. האימות החי בפועל קרה רק כתוצר לוואי של ניסיון לתקן פער אחר (M7). בפעם הבאה הייתי **בודק live-fire (session אמיתי, project dir נכון) כצעד הראשון בעבודה על harness, לא כבדיקה נלווית בסוף.**
- **`.claude/rules` עם `paths` לא חוצה את גבול תיקיית הפרויקט, וגם `.claude/settings.json` לא נטען אם ה-agent לא רץ מתוך אותה תיקייה.** `tax-data-sourcing.md` נכתב ל-`data/**/*.md` ולא מגיע ל-`TaxData/` שמחוץ ל-`tax-copilot/`; אבל הגילוי הרחב יותר מהסבב הזה הוא ש-`settings.json` הפרויקטי (hooks + permissions, M9–M11) גם הוא project-scoped באותו אופן — session שרץ עם cwd בשורש ה-repo לא טוען אותו בכלל. לא הייתי מפזר תשתית קריטית (rules, hooks, permissions) בלי לוודא במפורש מאיפה כל session אמור לרוץ.
- **סקריפט התקנה חיצוני (`specify init`) יכול לגעת בקבצים שלא ציפית להם.** ה-diff אחרי הרצת Spec Kit הראה `.claude/settings.json` כ-"modified" — התברר שזה רק שינוי line-ending (אומת byte-diff מול HEAD), אבל זה חייב בדיקה מפורשת ולא הנחה. בפעם הבאה הייתי מריץ `git diff` על כל קובץ ש-installer חיצוני נגע בו, לפני שממשיכים, במקום לבדוק רק את מה שהתכוונתי לשנות.
