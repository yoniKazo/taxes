# X4 — ריצה חיה אמיתית של `src/agent_team.py`

הרצה אחת מתועדת מתוך כמה, אחרי תיקון `max_tokens` (2000→8192) ותיקון prompt המיזוג לתקציר (top-10 לדוגמה + "ועוד N") כי ריצה ראשונה נקטעה באמצע משפט תחת `max_tokens=2000` עם 285 ממצאים גולמיים. `ANTHROPIC_API_KEY`/`GEMINI_API_KEY` אמיתיים, ללא mocks. עלות: **$0.0152** (Gemini workers: $0 — free tier lite; Anthropic lead: $0.0152, קריאה אחת ל-`claude-haiku-4-5`).

הערת אמינות: `income-tax-deductions-booklet-2026` הוא ה-PDF הרשמי עצמו, ולכן "חוסר ציטוט" בו לא תמיד אומר "לא-מאומת" באותה מידה כמו במסמכי md — זה מספר גבוה במיוחד (291) כי המסמך מציג טבלאות סכומים צפופות בלי לצטט את עצמו כמקור. ריצת ה-audit הידנית של X3 (subagents נפרדים) נתנה לפדף הזה הוראה שונה במפורש ("זהו המקור עצמו, דגול רק מה שקשה לשייך לסעיף") ולכן קיבלה מספר נמוך משמעותית (6) — הבדל תוצאה תקין בין שתי ריצות עם prompts שונים במכוון, לא באג.

---

```
# דוח ביקורת מסמכי מס - מספרים ללא מקור

## סיכום כללי
**סה"כ ממצאים: 327 מספרים ללא מקור**

| מסמך | מספר ממצאים |
|------|------------|
| employees-tax-guide | 0 |
| self-employed-tax-guide | 36 |
| capital-markets-tax-guide | 0 |
| real-estate-tax-guide | 0 |
| general-tax-guide | 0 |
| income-tax-deductions-booklet-2026 | 291 |
```

(דוח מלא, כולל שתי הטבלאות המפורטות, נשמר גם ב-`tests/`/היסטוריית העבודה של הסשן; התמצית למעלה מספיקה כ-evidence לריצה חיה מוצלחת.)
